#if !defined(HAMMING_H)
#define HAMMING_H

#include <string>

namespace hamming {

    int compute(const std::string &first_string, const std::string &second_string);

}  // namespace hamming

#endif // HAMMING_H